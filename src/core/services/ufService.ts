import useUfs from "../../hooks/useUfs";
import Uf from "../models/Uf";
import { UfException } from "../settings/functions";

const {ufs} = useUfs()

export default class ufServices {

    

    // constructor(props: Uf[]){
    //     this. =  props
    // }

    // existente(props: string){
    //     const existe = this._props.filter(uf => (uf.nomeUf === props))
    //     if(!existe){ 
    //         return existe
    //     } else{
    //         throw new UfException('UF existente')
    //     }
    // }
}